Useful Windows PowerShell commands
# Go to directory
cd "G:\My Drive\sync\air quality forecast\Git_repo"
cd "C:\aqf311\repo\stage1"

# Stage, commit and push changes
git add -A; git commit -m "Update project"; git push

# Check if Git is up to date both locally and online
git status

